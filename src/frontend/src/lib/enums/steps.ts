export enum SendStep {
	INITIALIZATION = 'initialization',
	SIGN = 'sign',
	SEND = 'send',
	APPROVE = 'approve',
	DONE = 'done'
}

export enum SignStep {
	INITIALIZATION = 'initialization',
	SIGN = 'sign',
	APPROVE = 'approve',
	DONE = 'done'
}

export enum BurnStep {
	INITIALIZATION = 'initialization',
	BURN = 'burn',
	DONE = 'done'
}
