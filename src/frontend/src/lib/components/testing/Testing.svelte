<script lang="ts">
	// Testing harness - DO NOT MERGE. Host for the harness scenarios: each one is a card, and each
	// card owns its own switches. Deliberately not translated - the page never reaches a user.
</script>

<p class="mb-6 text-secondary">
	Scenarios that make failure paths reachable on demand, so they can be checked without waiting for
	something to genuinely misbehave. Every scenario is off until you switch it on, and each one
	states when it takes effect.
</p>
