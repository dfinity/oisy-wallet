import type { TransactionFilterSettings } from '$declarations/backend/backend.did';

export const mockUserTransactionFilterSettings = {
	hideMicroTransactions: true
};

export const mockUserTransactionFilterSettingsMap: TransactionFilterSettings = {
	hide_micro_transactions: true
};
