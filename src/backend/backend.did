type Arg = variant { Upgrade; Init : InitArg };
type InitArg = record { ecdsa_key_name : text; chain_id : nat };
service : (Arg) -> {
  caller_eth_address : () -> (text);
  sign_transaction : () -> (text);
}